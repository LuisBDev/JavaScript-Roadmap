const product = "Monitor de 42 pulgadas";

console.log(product);

console.log(product.length); //includes spaces

console.log(product.indexOf("pulgadas")); //desde el 0 index
console.log(product.includes("42")); //boolean
